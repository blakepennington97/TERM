import 'package:flutter/material.dart';

class Education extends StatelessWidget {
 final Color color;

 Education(this.color);

 @override
 Widget build(BuildContext context) {
   return Container(
     color: color,
   );
 }
}